export const metadata = {
  title: 'Multibagger Stock Picks Under ₹100',
  description: 'High-potential multibagger stocks under ₹100 with entry points, targets, and stop losses',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, padding: 0 }}>{children}</body>
    </html>
  )
}
